name: CPU tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -e '.[dev,medical_io,flow]'
      - run: pytest -q
      - run: ruff check src tests scripts
      - run: python -m cardiac_agent.smoke --output runs/ci-smoke
